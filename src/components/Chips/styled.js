import styled from 'styled-components';

export default styled.div`
  display: inline-block;
  vertical-align: middle;
  padding-left: 0.5em;
  padding-right: 0.5em;
  -webkit-background-clip: unset;
  -webkit-text-fill-color: rgba(26,56,105, 0.8);
  color: rgba(26,56,105, 0.8);
  border-radius: 3px;
  font-size: 13px;
  line-height: 1.55em;
  white-space: normal;
  overflow: hidden;
  text-overflow: ellipsis;
  background: rgba(255,224,99,0.6);
`;
